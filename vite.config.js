import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  base: "/" // tells Vite to use root for asset paths (important for GitHub Pages deployment)
})
